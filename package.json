{
  "name": "joaoblasques.github.io",
  "private": true,
  "description": "joaoblasques.com — static site. Build-time only; nothing ships to the browser.",
  "scripts": {
    "build": "node build.js && node check.js",
    "test": "node check.js",
    "serve": "node build.js && npx --yes http-server docs -p 8080 -c-1",
    "refresh": "./refresh-contributions.sh && node build.js && node check.js"
  },
  "devDependencies": {
    "marked": "^18.0.6"
  }
}
